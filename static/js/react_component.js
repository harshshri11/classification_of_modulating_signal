class SignalClassifier extends React.Component {
    render() {
        return (
            <div>
                <h2>React Signal Classifier</h2>
                <p>React component replaces plain HTML</p>
            </div>
        );
    }
}

ReactDOM.render(<SignalClassifier />, document.getElementById("react-root"));
